# VK Token Bot

Telegram bot для получения токенов доступа VK через диалоговый интерфейс.

## Возможности

- 🔐 Авторизация в VK через диалоги Telegram
- 🤖 Автоматическое решение капчи через 2captcha
- 📱 Поддержка двухфакторной аутентификации (SMS)
- 🔒 Безопасная передача токена в Избранное VK
- 📝 Логирование всех операций

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd vk_me_v2
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

4. Заполните `.env` файл своими данными:
   - `TELEGRAM_TOKEN` - токен вашего Telegram бота (получите у [@BotFather](https://t.me/BotFather))
   - `RUCAPTCHA_API_KEY` - API ключ от [2captcha.com](https://2captcha.com)
   - `PROXY_ADDRESS`, `PROXY_PORT`, `PROXY_LOGIN`, `PROXY_PASSWORD` - данные прокси-сервера

## Использование

1. Запустите бота:
```bash
npm start
```

2. Откройте диалог с ботом в Telegram

3. Используйте команду `/start` для приветствия

4. Используйте команду `/login` для начала авторизации

5. Следуйте инструкциям бота:
   - Введите логин (телефон или email)
   - Введите пароль
   - При необходимости введите код из SMS

6. Токен будет отправлен в Избранное VK или в чат Telegram

## Структура проекта

```
.
├── bot.js              # Основной файл бота с диалоговой логикой
├── vkApi.js            # Модуль для работы с VK API
├── captchaSolver.js    # Модуль решения капчи
├── logger.js           # Модуль логирования
├── package.json        # Зависимости проекта
├── .env.example        # Пример конфигурации
└── README.md           # Документация
```

## Безопасность

⚠️ **Важно:**
- Никогда не публикуйте файл `.env` с реальными токенами
- Храните токены в безопасном месте
- Используйте бота только для личных целей
- Не передавайте свои данные третьим лицам

## Требования

- Node.js >= 14.x
- npm или yarn
- Telegram Bot Token
- 2captcha API Key
- Прокси-сервер (опционально, но рекомендуется)

## Лицензия

MIT

## Отказ от ответственности

Этот бот предназначен только для образовательных целей. Используйте его на свой страх и риск. Авторы не несут ответственности за любое неправомерное использование.
